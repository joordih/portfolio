{
  "name": "portfolio",
  "description": "Personal portfolio with blog, guestbook, and admin dashboard",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/joordih/portfolio.git"
  },
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev:api": "docker compose up --build mongo api",
    "dev:db": "docker compose up mongo",
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm run build --prefix client",
    "typecheck": "npm run typecheck --prefix client",
    "install:all": "npm install && npm install --prefix client"
  }
}
