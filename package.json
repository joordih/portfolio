{
  "name": "portfolio",
  "description": "Personal portfolio with blog, guestbook, and admin dashboard",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/joordih/portfolio.git"
  },
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev:db": "docker compose up -d mongo",
    "dev:api": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && node scripts/copy-api-lib.mjs && npm run build --prefix client",
    "build:vercel": "npm run build --prefix server && node scripts/copy-api-lib.mjs && npm run build --prefix client",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "dependencies": {
    "mongodb": "^6.15.0",
    "cookie-session": "^2.1.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.1.0"
  }
}